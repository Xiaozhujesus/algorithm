package priv.wz.array;

/**
 * 给你一个大小为 m x n 的矩阵 mat 和一个整数阈值 threshold。mat 中元素大于等于 0。
 * 请你返回元素总和小于或等于阈值的正方形区域的最大边长；如果没有这样的正方形区域，则返回 0
 */
public class MaxSideLength {
    public int maxSideLength(int[][] mat, int threshold) {
        return 0;
    }
}
